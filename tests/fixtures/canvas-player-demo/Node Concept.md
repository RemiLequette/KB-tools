---
style: "[[Shared Style]]"
fill: "#27AE60"
text: "Core Concept"
text-fr: "Concept clé"
---

Note: `image` intentionally omitted here — see `Node Conclusion.md` for the `icon` variant instead. An `image:` field was not stubbed in this demo since it needs a real vault image to resolve; flag if you want one added.
